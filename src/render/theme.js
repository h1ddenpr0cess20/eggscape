/** One palette, and it only has one colour in it — plus the two warnings. */
export const THEME = {
  void: 0x000000,
  wire: 0x00ff41,
  dim: 0x0a8f2a,
  faint: 0x063d14,
  agent: 0xff2b4a,
  bit: 0xc6ff3d,
  glow: 0x7dffa0,
};

export const CSS = {
  wire: '#00ff41',
  dim: '#0a8f2a',
  agent: '#ff2b4a',
};
